services:
  - type: web
    name: 42msg-relay
    runtime: python
    buildCommand: pip install websockets
    startCommand: python relay_server.py
    plan: free
    envVars:
      - key: PORT
        value: 10000
